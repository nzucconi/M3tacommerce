import React from 'react';
import './Item.css'

function ItemDetails(brand, title, price, src, stock, id ) {
    
return (
    <div className='detailButton'>
        <button>See Products Details</button>
    </div>
    )
} 
    
export default ItemDetails;